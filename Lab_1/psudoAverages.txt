

Main --------------

create 3 arrays of size 100
create counts for each

set array values to zero since c doesnt do that automatically

set count equal to numbers gathered from data

get_even_odd_sums() // organize data to requirements

if count / no data
print there is no average

else
 print averages for each depending on availability

return 0 in program


Main End ----------------------

int get_output(array to be added to)

count is zero
for each number typed into console, add to array and increment count

return count for main.

end------------------------

void get_even_odd_sums(gather relevent data from main array, its count, and the other two arrays, add a REFERENCE to their counts to manage later)

for each number in numbers
    set sum to zero
    set reference to number
    if reference is negative make array at i negative.
    while number is over 0
        keep adding each part of number to itself
    if sum is even_count
        add to even array 
        increment apporpriate count
    if odd
        do the same

return nothing

end--------------------------------

void take average(takes array and its size)

    if size is zero, return

    iterate through array adding all values (sum up)

    double average = (double)sum / size         (average basically)
    print to console the values

end ------------------------

void set_array(array, size, and value)

helper for loop that just makes array indexs equal to a value defined

end ------------------------------

void print_array(arrary, size)

helper for loop yo display contents of array

end ------------------



